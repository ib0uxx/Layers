{# filepath: c:\Users\Clem\Documents\github\Layers\templates\admin\products\index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Gestion des Produits{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>📦 Produits</h1>
        <a href="{{ path('app_admin_products_new') }}" class="btn btn-primary">+ Nouveau produit</a>
    </div>

    {% if products|length > 0 %}
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Nom</th>
                        <th>Catégorie</th>
                        <th>Prix</th>
                        <th>Statut</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for product in products %}
                        <tr>
                            <td><strong>{{ product.name }}</strong></td>
                            <td>
                                {% if product.categories|length > 0 %}
                                    {{ product.categories|first.name }}
                                {% else %}
                                    -
                                {% endif %}
                            </td>
                            <td>{{ product.price }} €</td>
                            <td>
                                <span class="badge {% if product.status.value == 'active' %}bg-success{% else %}bg-secondary{% endif %}">
                                    {{ product.status.value }}
                                </span>
                            </td>
                            <td>
                                <a href="{{ path('app_admin_products_edit', {id: product.id}) }}" class="btn btn-sm btn-warning">✏️ Éditer</a>
                                <form method="POST" action="{{ path('app_admin_products_delete', {id: product.id}) }}" style="display:inline;">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
                                    <button class="btn btn-sm btn-danger" onclick="return confirm('Êtes-vous sûr ?')">🗑️ Supprimer</button>
                                </form>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% else %}
        <div class="alert alert-info">Aucun produit trouvé.</div>
    {% endif %}
</div>
{% endblock %}