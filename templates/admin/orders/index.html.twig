{# filepath: c:\Users\Clem\Documents\github\Layers\templates\admin\orders\index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Gestion des Commandes{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>📋 Commandes</h1>

    {% if orders|length > 0 %}
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>#ID</th>
                        <th>Client</th>
                        <th>Date</th>
                        <th>Total Articles</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for order in orders %}
                        <tr>
                            <td><strong>#{{ order.id }}</strong></td>
                            <td>{{ order.user.email }}</td>
                            <td>{{ order.createdAt|date('d/m/Y H:i') }}</td>
                            <td>{{ order.orderItems|length }}</td>
                            <td>
                                <a href="{{ path('app_admin_orders_show', {id: order.id}) }}" class="btn btn-sm btn-info">👁️ Voir</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% else %}
        <div class="alert alert-info">Aucune commande trouvée.</div>
    {% endif %}
</div>
{% endblock %}